import { LoggingDirective } from './logging.directive';

describe('LoggingDirective', () => {
  it('should create an instance', () => {
    const directive = new LoggingDirective();
    expect(directive).toBeTruthy();
  });
});
